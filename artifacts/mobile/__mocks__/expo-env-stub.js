// Stub for expo/virtual/env — provides CJS-compatible env access so that
// babel-preset-expo's injected import of this module doesn't crash the
// Jest node environment with an "Unexpected token 'export'" error.
module.exports = { env: process.env };
